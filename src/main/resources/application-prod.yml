spring:

  datasource:
    url: jdbc:mysql://prod-db.xxx.amazonaws.com:3306/hhplus
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  data:
    redis:
      host: redis-prod

  kafka:
    bootstrap-servers: kafka-prod:9092